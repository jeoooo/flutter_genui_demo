name: bwai_flutter_genui_demo
description: "bwai_flutter_genui_demo"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  flutter_bloc: ^9.1.1
  genui: ^0.9.0
  google_generative_ai: ^0.4.0
  equatable: ^2.0.7
  json_schema_builder: ^0.1.3
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
  assets:
    - .env
